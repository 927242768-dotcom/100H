# 第三方组件说明

PDS、PCIe IP、MNN、HyperLPR、RKNN Toolkit Lite2 和 YOLO 模型均受各自许可证或厂商条款约束。

仓库不重新分发带有 PANGO 私有源码声明的完整 PCIe IP RTL，只保留项目配置、自研 RTL、集成说明和最终烧录文件。

`libhyperlpr3.so` 与 `libMNN.so` 需要按 `docs/mnn_hyperlpr_integration.md` 在目标板构建或安装。
